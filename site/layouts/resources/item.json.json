{
    "name": "{{ .Params.title }}",
    "slug" : "{{ .Params.slug }}",
    "description" : "{{ .Params.description }}",
    "tags" : "{{ .Params.tags }}",
    "links" : "{{ .Params.links }}",
    "files" : "{{ .Params.files }}"
}